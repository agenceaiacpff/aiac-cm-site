"use client";

import DOMPurify from "dompurify";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
import { createClient } from "@/lib/supabase/client";

type Payload={id:string;title:string;file_name:string;classification:string;html_content:string;can_download:boolean;viewer_name:string;secure_view_only:boolean};
export default function SecureDocumentReader({documentId}:{documentId:string}){const supabase=useMemo(()=>createClient(),[]);const[data,setData]=useState<Payload|null>(null);const[notice,setNotice]=useState('');useEffect(()=>{void(async()=>{const{data:d,error}=await supabase.rpc('institutional_document_secure_preview',{target_document_id:documentId});if(error)setNotice(error.message);else setData(d as Payload);})();},[documentId]);useEffect(()=>{if(!data?.secure_view_only)return;const block=(e:Event)=>e.preventDefault();const key=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&['p','s','c','u'].includes(e.key.toLowerCase())){e.preventDefault();e.stopPropagation();}};document.addEventListener('contextmenu',block);document.addEventListener('copy',block);document.addEventListener('dragstart',block);window.addEventListener('keydown',key,true);return()=>{document.removeEventListener('contextmenu',block);document.removeEventListener('copy',block);document.removeEventListener('dragstart',block);window.removeEventListener('keydown',key,true);};},[data?.secure_view_only]);if(notice)return <main className="secureReader"><p>{notice}</p><Link href="/espace/poste?section=resources">Retour aux ressources</Link></main>;if(!data)return <main className="secureReader"><p>Chargement sécurisé…</p></main>;const safe=DOMPurify.sanitize(data.html_content||'');return <main className={`secureReader ${data.secure_view_only?'protected':''}`}><style>{`body{margin:0;background:#eef2f7}.secureReader{max-width:1120px;margin:0 auto;padding:24px;font-family:Arial,sans-serif}.readerTop{position:sticky;top:0;z-index:20;background:#fff;border:1px solid #dbe2ea;border-radius:14px;padding:14px 18px;display:flex;gap:12px;justify-content:space-between;align-items:center;box-shadow:0 8px 25px #0001}.readerActions{display:flex;gap:8px;flex-wrap:wrap}.readerActions a,.readerActions button{padding:9px 12px;border-radius:9px;border:0;background:#123b6d;color:white;text-decoration:none;cursor:pointer}.paper{position:relative;margin-top:18px;background:#fff;border:1px solid #dce3ea;padding:40px;min-height:70vh;box-shadow:0 8px 30px #0001;overflow:auto}.paper table{border-collapse:collapse;width:100%;margin:12px 0}.paper td,.paper th{border:1px solid #bbb;padding:6px;vertical-align:top}.paper pre{white-space:pre-wrap;font-family:inherit}.protected .paper{user-select:none;-webkit-user-select:none}.watermarks{pointer-events:none;position:absolute;inset:0;overflow:hidden;z-index:10}.watermarks span{position:absolute;transform:rotate(-28deg);font-size:24px;font-weight:700;color:rgba(120,0,0,.08);white-space:nowrap}.content{position:relative;z-index:5}@media print{body>*{display:none!important}}`}</style><header className="readerTop"><div><b>{data.title}</b><div><small>{data.file_name} · {data.classification}{data.secure_view_only?' · consultation protégée':''}</small></div></div><div className="readerActions"><Link href="/espace/poste?section=resources">Retour</Link>{data.can_download&&<button onClick={()=>window.open(`/api/documents/${documentId}/download`,'_blank','noopener,noreferrer')}>Télécharger l’original</button>}</div></header><section className="paper">{data.secure_view_only&&<div className="watermarks" aria-hidden="true">{Array.from({length:24},(_,i)=><span key={i} style={{left:`${(i%4)*27-10}%`,top:`${Math.floor(i/4)*18+4}%`}}>AIAC · {data.viewer_name} · consultation tracée</span>)}</div>}<div className="content">{safe?<div dangerouslySetInnerHTML={{__html:safe}}/>:<><p>Aucun aperçu HTML n’a encore été généré pour ce fichier.</p><iframe title={data.title} src={`/api/documents/${documentId}/view`} style={{width:'100%',height:'75vh',border:0}}/></>}</div></section>{data.secure_view_only&&<p><small>Mesures de dissuasion actives : filigrane nominatif, copie/impression/enregistrement navigateur neutralisés et consultation journalisée. Un appareil externe ou une capture au niveau du système d’exploitation ne peut pas être techniquement empêché de façon absolue par une application web.</small></p>}</main>}
